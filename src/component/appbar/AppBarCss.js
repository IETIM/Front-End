
import { makeStyles } from '@material-ui/core/styles';

export const AppBarCss = makeStyles(() => ({
    sidebarpage: {
        backgroundColor: '#060b26',
        height: '80px',
        display: 'flex',
        justifyContent: 'start',
        alignItems: 'center',
        width: '100%',
        position: 'fixed'
    }

}));